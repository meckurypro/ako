import{useState}from"react";import{Pressable,View}from"react-native";import{Text}from"@/components/core";const PARTS=/(#[\p{L}\p{N}_]+)/gu;
export function PostText({heading,content}:{heading?:string|null;content:string}){const[expanded,setExpanded]=useState(false);const parts=content.split(PARTS);return <View style={{gap:8}}>{heading&&<Text variant="heading">{heading}</Text>}<Text numberOfLines={expanded?undefined:6} style={{fontSize:16,lineHeight:24}}>{parts.map((part,index)=>part.startsWith("#")?<Text key={`${part}-${index}`} color="accent">{part}</Text>:part)}</Text>{content.length>280&&<Pressable accessibilityRole="button" onPress={()=>setExpanded(value=>!value)}><Text variant="label" color="accent">{expanded?"Show less":"Show more"}</Text></Pressable>}</View>}

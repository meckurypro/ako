import { useMemo, useState } from "react";
import { ActivityIndicator, Image, Modal, Pressable, ScrollView, StyleSheet, View } from "react-native";
import { MaterialCommunityIcons } from "@expo/vector-icons";
import { Avatar, Text } from "@/components/core";
import { type MobileGiftType, useGiftTypes, useSendGift, useWallet } from "@/features/feed/api";
import { useTheme } from "@/providers/ThemeProvider";

type Step = "catalog" | "confirm" | "sent";
const imageUrl = (value: string | null) => !value ? null : value.startsWith("http") ? value : `https://ako-taupe.vercel.app${value.startsWith("/") ? value : `/gifts/${value}`}`;
export function GiftPicker({ recipientId, recipientName, recipientAvatar, postId, onClose }: { recipientId: string; recipientName: string; recipientAvatar: string | null; postId?: string; onClose: () => void }) {
  const { colors } = useTheme(); const gifts = useGiftTypes(); const wallet = useWallet(); const send = useSendGift(); const [step, setStep] = useState<Step>("catalog"); const [selected, setSelected] = useState<MobileGiftType | null>(null); const [error, setError] = useState<string | null>(null);
  const balance = Number(wallet.data?.balance ?? 0); const sorted = useMemo(() => [...(gifts.data ?? [])].sort((a, b) => b.cost_usd - a.cost_usd), [gifts.data]);
  const pick = (gift: MobileGiftType) => { if (balance < gift.cost_usd) { setError(`Insufficient balance to gift ${gift.name}. Please fund your wallet.`); return; } setError(null); setSelected(gift); setStep("confirm"); };
  const confirm = async () => { if (!selected) return; setError(null); try { await send.mutateAsync({ recipient_id: recipientId, gift_type_id: selected.id, ...(postId ? { post_id: postId } : {}) }); setStep("sent"); } catch (cause) { setError(cause instanceof Error ? cause.message : "Couldn’t send gift."); } };
  return <Modal visible transparent animationType="fade" onRequestClose={onClose}><View style={s.root}><Pressable style={[s.backdrop, { backgroundColor: colors.overlay }]} onPress={onClose} /><View style={[s.sheet, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={[s.header, { borderBottomColor: colors.border }]}>{step === "confirm" ? <Pressable onPress={() => setStep("catalog")} style={s.headerButton}><MaterialCommunityIcons name="arrow-left" size={21} color={colors.textMuted} /></Pressable> : <View><Text style={s.title}>Send a gift</Text><Text color="muted" style={s.subhead}>Send a piece of heritage.</Text></View>}<View style={s.headerRight}>{step === "catalog" && <Text color="muted" style={s.balance}>Balance <Text style={s.balanceValue}>${balance.toFixed(2)}</Text></Text>}<Pressable onPress={onClose} style={s.headerButton}><MaterialCommunityIcons name="close" size={21} color={colors.textMuted} /></Pressable></View></View><ScrollView contentContainerStyle={s.content}>{step === "catalog" ? gifts.isLoading ? <ActivityIndicator color={colors.accent} style={s.loader} /> : <View style={s.grid}>{sorted.map(gift => <Pressable key={gift.id} onPress={() => pick(gift)} style={s.gift}><GiftImage gift={gift} /><Text color="muted" style={s.price}>${gift.cost_usd.toFixed(2)}</Text></Pressable>)}</View> : step === "confirm" && selected ? <View style={s.confirm}><GiftImage gift={selected} large /><Text style={s.confirmTitle}>Send {selected.name}</Text><View style={s.recipient}><Avatar uri={recipientAvatar} name={recipientName} size={28} /><Text color="muted" style={s.toText}>to {recipientName}</Text></View><View style={[s.receipt, { backgroundColor: colors.background, borderColor: colors.border }]}><Receipt label="Gift value" value={`$${selected.cost_usd.toFixed(2)}`} /><View style={[s.rule, { backgroundColor: colors.border }]} /><Receipt label="Your balance" value={`$${balance.toFixed(2)}`} /></View>{error && <Text color="danger" style={s.error}>{error}</Text>}</View> : selected ? <View style={s.sent}><GiftImage gift={selected} large /><Text style={s.confirmTitle}>Gift sent!</Text><Text color="muted" align="center" style={s.sentCopy}>You sent a {selected.name} to {recipientName}.{`\n`}${selected.cost_usd.toFixed(2)} deducted from your wallet.</Text></View> : null}</ScrollView>{step === "confirm" && selected && <View style={[s.footer, { borderTopColor: colors.border }]}><Pressable onPress={() => setStep("catalog")} style={[s.footerButton, { borderColor: colors.border }]}><Text style={s.buttonText}>Cancel</Text></Pressable><Pressable disabled={send.isPending} onPress={() => void confirm()} style={[s.footerButton, { backgroundColor: colors.accent, opacity: send.isPending ? .55 : 1 }]}>{send.isPending ? <ActivityIndicator size="small" color="#07130D" /> : <Text style={[s.buttonText, { color: "#07130D" }]}>Send gift</Text>}</Pressable></View>}{step === "sent" && <View style={[s.footer, { borderTopColor: colors.border }]}><Pressable onPress={onClose} style={[s.done, { backgroundColor: colors.accent }]}><Text style={[s.buttonText, { color: "#07130D" }]}>Done</Text></Pressable></View>}</View></View></Modal>;
}
function GiftImage({ gift, large }: { gift: MobileGiftType; large?: boolean }) { const uri = imageUrl(gift.icon_url); return <View style={large ? s.giftLarge : s.giftImage}>{uri ? <Image source={{ uri }} style={s.image} resizeMode="contain" /> : <Text style={large ? s.fallbackLarge : s.fallback}>🎁</Text>}</View>; }
function Receipt({ label, value }: { label: string; value: string }) { return <View style={s.receiptRow}><Text color="muted">{label}</Text><Text style={s.receiptValue}>{value}</Text></View>; }
const s = StyleSheet.create({ root: { flex: 1, justifyContent: "flex-end" }, backdrop: { ...StyleSheet.absoluteFill }, sheet: { maxHeight: "85%", borderWidth: StyleSheet.hairlineWidth, borderBottomWidth: 0, borderTopLeftRadius: 24, borderTopRightRadius: 24, overflow: "hidden" }, header: { minHeight: 67, paddingHorizontal: 16, paddingVertical: 11, borderBottomWidth: StyleSheet.hairlineWidth, flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, title: { fontFamily: "serif", fontSize: 19, fontWeight: "700" }, subhead: { fontSize: 12, marginTop: 2 }, headerRight: { flexDirection: "row", alignItems: "center", gap: 10 }, balance: { fontSize: 12 }, balanceValue: { color: "#EEEDE9", fontWeight: "700" }, headerButton: { width: 32, height: 32, alignItems: "center", justifyContent: "center" }, content: { padding: 16 }, loader: { marginVertical: 60 }, grid: { flexDirection: "row", flexWrap: "wrap", justifyContent: "space-between", rowGap: 12 }, gift: { width: "30%", alignItems: "center", gap: 5, paddingVertical: 3 }, giftImage: { width: 78, height: 78, alignItems: "center", justifyContent: "center" }, giftLarge: { width: 92, height: 92, alignItems: "center", justifyContent: "center" }, image: { width: "100%", height: "100%" }, fallback: { fontSize: 34 }, fallbackLarge: { fontSize: 46 }, price: { fontSize: 12 }, confirm: { alignItems: "center", gap: 14, paddingVertical: 8 }, confirmTitle: { fontFamily: "serif", fontSize: 20, fontWeight: "700" }, recipient: { flexDirection: "row", alignItems: "center", gap: 8 }, toText: { fontSize: 14 }, receipt: { width: "100%", borderWidth: 1, borderRadius: 13, padding: 15, gap: 12, marginTop: 3 }, receiptRow: { flexDirection: "row", justifyContent: "space-between" }, receiptValue: { fontWeight: "700" }, rule: { height: StyleSheet.hairlineWidth }, error: { fontSize: 13, textAlign: "center" }, sent: { alignItems: "center", gap: 13, paddingVertical: 35 }, sentCopy: { fontSize: 14, lineHeight: 20 }, footer: { padding: 14, flexDirection: "row", gap: 10, borderTopWidth: StyleSheet.hairlineWidth }, footerButton: { flex: 1, height: 46, borderRadius: 24, borderWidth: 1, alignItems: "center", justifyContent: "center" }, done: { flex: 1, height: 46, borderRadius: 24, alignItems: "center", justifyContent: "center" }, buttonText: { fontSize: 14, fontWeight: "700" } });
